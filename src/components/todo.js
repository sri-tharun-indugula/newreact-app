import React from "react";


// The Header creates links that can be used to navigate
// between routes.
const Todo = () => (
    <div>
        <label>hello</label>
        <input></input>
    </div>
);

export default Todo;